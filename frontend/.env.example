# Optional Supabase values. The local sign-practice demo can render without
# these values, but login/signup needs a real Supabase project.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=http://localhost:4000
